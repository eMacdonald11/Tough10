<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Mobile-first single-column layout, earthy tones, cohesive design */
    body {
      font-family: 'Open Sans', sans-serif;
      text-align: center;
      background: #F9F8F3;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      overflow-x: hidden;
    }
    h1 {
      color: #556B2F;
      margin-bottom: 20px;
      font-size: 24px;
    }
    p {
      font-size: 16px;
      margin: 10px 0;
      color: #333;
    }
    .error {
      color: red;
    }
    form {
      margin: 20px 0;
    }
    label {
      display: block;
      margin: 10px 0 5px;
      font-weight: 600;
      text-align: left;
      max-width: 300px;
      margin: 10px auto 5px;
    }
    input[type="email"], input[type="password"] {
      width: 100%;
      max-width: 300px;
      padding: 10px;
      font-size: 16px;
      border: 1px solid #CCC;
      border-radius: 5px;
      margin-bottom: 15px;
    }
    button {
      display: block;
      width: 100%;
      max-width: 300px;
      margin: 10px auto;
      padding: 15px 25px;
      font-size: 16px;
      font-weight: 700; /* Bold text */
      background: #A0522D;
      color: #fff;
      border: 2px solid #8C4528; /* Slight trim matching color scheme */
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
    }
    button:hover {
      background: #8C4528;
    }
    a {
      color: #556B2F;
      text-decoration: none;
      font-weight: 600;
    }
    a:hover {
      text-decoration: underline;
    }
    @media (max-width: 600px) {
      body {
        padding: 10px;
      }
      label, input, button {
        max-width: 100%;
      }
    }
  </style>
</head>
<body>
  <!-- Login page: Compressed single column, consistent color scheme. -->

  <h1>Login</h1>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <form action="/login" method="POST">
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required>

    <button type="submit">Login</button>
  </form>

  <p>Don't have an account? <a href="/register">Register here</a></p>
</body>
</html>