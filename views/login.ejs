<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Added viewport meta tag -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Mobile-first single-column layout, earthy tones, cohesive design */
    body {
      font-family: 'Open Sans', sans-serif;
      text-align: center;
      background: #F9F8F3;
      color: #333;
      max-width: 600px; /* Changed from 600px to 100% */
      margin: 0 auto;
      padding: 2rem; /* Changed from 20px to rem units */
      box-sizing: border-box;
      overflow-x: hidden;
    }
    h1 {
      color: #556B2F;
      margin-bottom: 1.25rem; /* Changed from 20px to rem */
      font-size: 1.5rem; /* Changed from 24px to 1.5rem */
    }
    p {
      font-size: 1rem; /* Changed from 16px to 1rem */
      margin: 0.625rem 0; /* Changed from 10px to rem units */
      color: #333;
    }
    .error {
      color: red;
      font-size: 0.875rem; /* Changed from default */
      margin-bottom: 1rem;
    }
    form {
      margin: 1.25rem 0; /* Changed from 20px to rem units */
    }
    label {
      display: block;
      margin: 0.625rem auto 0.3125rem; /* Changed from 10px 0 5px to rem units */
      font-weight: 600;
      text-align: left;
      max-width: 100%; /* Changed from 300px to 100% */
      width: 100%;
      padding: 0 1rem; /* Added padding for better alignment */
      box-sizing: border-box;
    }
    input[type="email"], input[type="password"] {
      width: 100%;
      max-width: 100%; /* Changed from 300px to 100% */
      padding: 0.625rem; /* Changed from 10px to rem units */
      font-size: 1rem; /* Changed from 16px to 1rem */
      border: 1px solid #CCC;
      border-radius: 5px;
      margin-bottom: 0.9375rem; /* Changed from 15px to rem units */
      box-sizing: border-box;
    }
    button {
      display: block;
      width: 100%;
      max-width: 100%; /* Changed from 300px to 100% */
      margin: 0.625rem auto; /* Changed from 10px to rem units */
      padding: 0.9375rem 1.5625rem; /* Changed from 15px 25px to rem units */
      font-size: 1rem; /* Changed from 16px to 1rem */
      font-weight: 700; /* Bold text */
      background: #A0522D;
      color: #fff;
      border: 2px solid #8C4528; /* Slight trim matching color scheme */
      border-radius: 5px;
      cursor: pointer;
      transition: background 0.3s ease;
      box-sizing: border-box;
    }
    button:hover {
      background: #8C4528;
    }
    a {
      color: #556B2F;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem; /* Changed from default */
    }
    a:hover {
      text-decoration: underline;
    }
    @media (min-width: 601px) {
      /* Adjust styles for larger screens if needed */
      body {
        padding: 2rem;
      }
    }

    @media (max-width: 600px) {
      body {
        padding: 1rem;
      }
      label, input, button {
        max-width: 100%;
      }
    }
  </style>
</head>
<body>
  <!-- Login page: Compressed single column, consistent color scheme. -->

  <h1>Login</h1>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <form action="/login" method="POST">
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" required>

    <label for="password">Password:</label>
    <input type="password" id="password" name="password" required>

    <button type="submit">Login</button>
  </form>

  <p>Don't have an account? <a href="/register">Register here</a></p>
</body>
</html>